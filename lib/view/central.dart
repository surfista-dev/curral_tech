import 'package:flutter/material.dart';

class PageCental extends StatelessWidget {
  const PageCental({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
